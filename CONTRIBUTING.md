# 기여하기

버그 수정, 접근성 개선, 문서, 번역과 사용 사례를 환영합니다.

1. 저장소를 fork하고 변경용 브랜치를 만듭니다.
2. `npm ci`로 설치하고 `npm run dev`로 실행합니다.
3. 변경 전후의 동작과 검사 결과를 적어 pull request를 보냅니다.

```sh
npm test
npm run typecheck
npm run build
```

`content/book.json`은 배포 원본이자 patch의 기준입니다. 이 파일의 경로와 직렬화 형식을 바꿀 때는 patch 적용 테스트와 이전 버전 호환성을 함께 확인해 주세요. 새로운 MDX 컴포넌트는 명시적으로 등록하며 업로드 원고에서 임의 JavaScript를 실행하지 않습니다.

사이트는 한 권의 책을 위한 템플릿입니다. 회원가입, 결제, 외부 API 등 새 의존성이 큰 기능은 먼저 issue에서 제안해 주세요. 사용자 원고나 인증 토큰을 예제·테스트에 포함하지 마세요.
